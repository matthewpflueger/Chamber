<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
	    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	    xmlns:context="http://www.springframework.org/schema/context"
	    xmlns:util="http://www.springframework.org/schema/util"
	    xsi:schemaLocation="
	        http://www.springframework.org/schema/beans
	        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	        http://www.springframework.org/schema/context
	        http://www.springframework.org/schema/context/spring-context-3.0.xsd
	        http://www.springframework.org/schema/util
	        http://www.springframework.org/schema/util/spring-util-3.0.xsd"
	    default-init-method="init"
	    default-destroy-method="destroy">


    <!--suppress SpringModelInspection -->
    <context:property-placeholder
                location="classpath:servlet-context.properties"
                ignore-resource-not-found="true"
                properties-ref="servletContextProperties"/>
    <!--suppress SpringModelInspection -->
    <util:properties id="servletContextProperties">
        <prop key="mustache.cache">true</prop>
        <prop key="mustache.prefix">${urls.views}/</prop>
        <prop key="mustache.suffix">.html</prop>

        <prop key="echoUrl">${urls.site}/echo</prop>
        <prop key="echoItUrl">${urls.site}/echo/it</prop>
        <prop key="buttonUrl">${urls.site}/echo/button</prop>
        <prop key="closetUrl">${urls.site}</prop>

        <prop key="buttonViewUrl">${urls.images}/button_echoed.png</prop>
        <prop key="loginViewUrl">${urls.views}/view.popuplogin.html</prop>
        <prop key="errorViewUrl">${urls.views}/view.error.html</prop>
        <prop key="closetViewUrl">${urls.views}/view.user.html</prop>

        <prop key="twitterController.loginRedirectUrl">http://www.twitter.com</prop>

        <prop key="confirmViewUrl">${urls.views}/view.confirm.html</prop>


        <prop key="twitterLoginErrorViewUrl">${urls.views}/view.twitter_login_error.html</prop>
        <prop key="facebookLoginErrorViewUrl">${urls.views}/view.facebook_login_error.html</prop>
        <prop key="dashboardViewUrl">${urls.views}/view.partner.html</prop>

        <prop key="exhibitUrl">${urls.api}/user/exhibit</prop>
        <prop key="friendsUrl">${urls.api}/user/friends</prop>
        <prop key="dashboardUrl">${urls.site}/partner/dashboard</prop>
    </util:properties>

</beans>
